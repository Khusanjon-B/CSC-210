/*
Program Name:

Description:

Author:

Last Modified:

*/


Scanner:

    import java.util.Scanner;

    Scanner scanner1 = new Scanner(System.in);

    variable = scanner1.nextLine();

    variable = Integer.parseInt(scanner1.nextLine());

    for (int i = 0; i < 3; i++) {
        variable[i] = scanner.nextInt();
    }

    scanner1.close();

Arrays:

    import java.util.Arrays;

    Arrays.sort(integerValuesAscending);

    System.out.println("Ascending: " + Arrays.toString(integerValuesAscending));